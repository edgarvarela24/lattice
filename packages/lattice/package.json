{
  "name": "lattice",
  "version": "0.1.0",
  "description": "Lattice — Compiled TypeScript UI framework built on fine-grained signals",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@lattice/runtime": "workspace:*",
    "@lattice/vite": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
